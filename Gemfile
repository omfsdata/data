source "https://rubygems.org"

# Mirrors the exact Jekyll setup GitHub Pages uses, so a local preview
# matches production. NOT required for deployment — GitHub builds the site
# for you. Only needed if you want to run `bundle exec jekyll serve` locally.
gem "github-pages", group: :jekyll_plugins
